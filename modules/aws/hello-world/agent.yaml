version: v1
metadata:
  foo: bar
  input: "{{ .input.world }}"
  nested_input: "{{ .input.nest.egg }}"
  nested_array: "{{ .input.array.0 }}"
  prefix: "{{ .config.prefix }}"
  out: "{{ .sout.hello_world }}"
  ui_url: "https://{{ .input.nest.egg }}"
module_types: [hello-world]
